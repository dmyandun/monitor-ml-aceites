# Core
anthropic>=0.40.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-dotenv>=1.0.0

# Telegram
python-telegram-bot>=21.0.0

# Supabase
supabase>=2.9.0

# HTTP — upper bound por compatibilidad con supabase>=2.9.0
httpx>=0.27.0,<0.29.0

# Pydantic — versiones exactas para garantizar wheel binario pre-compilado.
# pydantic-core y jiter estan escritos en Rust; sin pin exacto pip puede
# resolver a una version mas nueva sin wheel disponible y quedar compilando.
pydantic==2.11.3
pydantic-core==2.33.1
jiter==0.13.0

# ─── NO incluir aqui prophet/pandas/numpy/scikit-learn ───────────────────────
# El HF Space solo LEE forecasts de Supabase — no entrena modelos.
# El entrenamiento corre localmente o en GitHub Actions con requirements-ml.txt
